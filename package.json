{
  "name": "adobe-to-mixpanel",
  "version": "1.0.0",
  "description": "ETL from adbobe omniture to mixpanel",
  "main": "index.js",
  "scripts": {
    "dev": "npm run prune && NODE_ENV=dev nodemon --ignore tmp/ --ignore fake/ --ignore sampleData/ --inspect scratch.mjs | bunyan -o short",
    "func": "NODE_ENV=prod functions-framework --target=start --source . --signature-type=http | bunyan -o short",
    "localDeploy": "gcloud alpha functions local deploy adobe_local --entry-point=start --runtime=nodejs18  --env-vars-file .env.yaml",
    "localRun": "gcloud alpha functions local call adobe_local --data='{ \"cloud_path\": \"gs://nykaa-adobe/testStuff/SRC/SMALL-SAMPLE.tsv.gz\", \"dest_path\": \"gs://nykaa-adobe/testStuff/MIRROR\" }'",
    "deploy": "gcloud alpha functions deploy nykaa-transform --gen2 --update-labels snowcat=transformer  --no-allow-unauthenticated  --env-vars-file .env.yaml --runtime nodejs18 --region us-central1 --trigger-http --memory 4GB --entry-point start --source . --timeout=3600 --max-instances=3355 --min-instances=0 --concurrency=1",
    "prune": "rm -rf ./tmp/*",
    "buildReq": "nodemon --ignore tmp/ request-builder.mjs",
    "express": "node index.js",
    "dockerBuild": "DOCKER_BUILDKIT=1 docker build -t us.gcr.io/mixpanel-gtm-training/nykaa-adobe-transform:dev .",
    "dockerPush": "docker push us.gcr.io/mixpanel-gtm-training/nykaa-adobe-transform:dev"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ak--47/adobeToMixpanel.git"
  },
  "type": "module",
  "author": "AK",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ak--47/adobeToMixpanel/issues"
  },
  "homepage": "https://github.com/ak--47/adobeToMixpanel#readme",
  "devDependencies": {
    "nodemon": "^2.0.19"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.2.0",
    "@google-cloud/logging-bunyan": "^4.2.2",
    "@google-cloud/storage": "^6.11.0",
    "ak-sheets": "^1.0.3",
    "ak-tools": "1.0.44",
    "bunyan": "^1.8.15",
    "bunyan-format": "^0.2.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "md5": "2.3.0",
    "node-gzip": "1.1.2",
    "papaparse": "5.3.2",
    "pino-pretty": "^13.0.0"
  }
}
